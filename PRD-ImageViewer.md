# PRD: Glance 开图功能

## 1. 背景与动机

Glance 当前是一款通过全局热键快速预览选中文本/路径的效率工具。用户反馈希望 Glance 能进一步承担「轻量看图」的角色：

- **痛点 1**：macOS 自带 Preview.app 启动慢、功能臃肿，只想快速看一眼图片细节
- **痛点 2**：对比两张相似图片（如设计稿 A/B 版本、修图前后）需要反复切换窗口，无法同屏并排
- **痛点 3**：想快速查看图片 EXIF/分辨率/体积等元数据，Preview.app 需要多次点击

## 2. 目标用户

- 设计师：频繁对比设计稿迭代版本
- 摄影师：快速筛选照片、查看 EXIF 参数
- 开发者：对比 UI 截图、检查素材尺寸
- 普通用户：替代 Preview.app 的轻量看图需求

## 3. 产品目标

1. **快速开图**：双击图片文件即可用 Glance 打开，秒开无延迟
2. **多图并排对比**：同一窗口内 2~4 张图并排显示，缩放/平移同步联动
3. **元数据速查**：一键查看分辨率、EXIF、GPS 等详细信息

## 4. 功能范围

### 4.1 In Scope (本期实现)

| 功能 | 描述 | 优先级 |
|------|------|--------|
| 文件关联打开 | 双击图片文件用 Glance 打开 | P0 |
| 单图查看器 | 支持缩放、平移、Fit-to-View | P0 |
| 多图并排对比 | 2~4 张图左右/网格布局，视口同步 | P0 |
| 元数据面板 | 右侧抽屉显示 EXIF/分辨率/体积 | P1 |
| 热键流程集成 | 选中多图路径时提供「对比」入口 | P1 |

### 4.2 Out of Scope (不做)

- 图片编辑（裁剪、调色、标注）
- 批量重命名/格式转换
- 幻灯片播放
- 云端图片加载

## 5. 用户故事与场景

### 场景 1：双击快速看图
> 作为设计师，我双击一张 PNG 文件，Glance 立即打开显示图片，我可以滚轮缩放查看细节，按 Cmd+I 查看尺寸信息，按 Esc 关闭。

### 场景 2：对比设计稿版本
> 作为设计师，我在 Finder 框选 v1.png 和 v2.png，右键用 Glance 打开。两张图左右并排显示，我拖拽其中一张，另一张同步移动，方便我对比按钮位置是否对齐。

### 场景 3：筛选照片查看 EXIF
> 作为摄影师，我双击一张 RAW 转出的 JPEG，按 Cmd+I 打开元数据面板，快速确认光圈 f/2.8、ISO 400、焦段 85mm，决定是否保留这张照片。

### 场景 4：热键流程中对比
> 作为开发者，我在 Slack 消息里看到两个截图路径，选中后按 Option 键，Glance 弹出预览，我点击「对比」按钮，两张截图并排显示。

## 6. 交互设计

### 6.1 入口方式

```
┌─────────────────────────────────────────────────────────┐
│  入口                        │  行为                    │
├─────────────────────────────────────────────────────────┤
│  双击单张图片                 │  打开单图查看器           │
│  框选多张图片 → 右键 Glance   │  打开并排对比模式         │
│  热键选中多图路径             │  PreviewPanel 显示对比按钮│
└─────────────────────────────────────────────────────────┘
```

### 6.2 单图查看器

```
┌──────────────────────────────────────────┐
│  [−] [100%] [+]  [⊞]  [i]          [×]  │  ← 工具栏
├──────────────────────────────────────────┤
│                                          │
│                                          │
│              [ 图片内容 ]                 │
│                                          │
│                                          │
└──────────────────────────────────────────┘
```

点击 [i] 按钮，右侧抽屉滑出显示元数据：

```
┌──────────────────────────────────────────┬──────────────────────┐
│  [−] [100%] [+]  [⊞]  [i]          [×]  │                      │
├──────────────────────────────────────────┤    图片信息          │
│                                          │                      │
│                                          │  尺寸   1920 × 1080  │
│              [ 图片内容 ]                 │  体积   2.4 MB       │
│                                          │  格式   PNG          │
│                                          │  色彩   sRGB         │
│                                          │  ...                 │
└──────────────────────────────────────────┴──────────────────────┘
                                            ↑ 右侧抽屉（动画滑出）
```

### 6.3 并排对比模式

```
┌─────────────────────────────────────────────────────────┐
│  [🔗 锁定] [⊞ 布局] [i]                           [×]  │
├────────────────────────┬────────────────────────────────┤
│                        │                                │
│                        │                                │
│      [ 图片 A ]        │         [ 图片 B ]             │
│                        │                                │
│                        │                                │
└────────────────────────┴────────────────────────────────┘

- 🔗 锁定：缩放/平移同步（默认开启）
- ⊞ 布局：切换左右 ↔ 上下 ↔ 2×2 网格
```

点击 [i] 按钮，右侧抽屉滑出，**信息面板布局与图片布局镜像对应**：

**双图左右并排 → 信息左右并排：**
```
┌────────────────────────┬────────────────────────┬─────────────┬─────────────┐
│                        │                        │  图片 A 信息 │  图片 B 信息 │
│      [ 图片 A ]        │      [ 图片 B ]        │  1920×1080  │  1920×1080  │
│                        │                        │  2.4 MB     │  2.1 MB     │
│                        │                        │  PNG        │  PNG        │
└────────────────────────┴────────────────────────┴─────────────┴─────────────┘
                                                   ↑ 右侧抽屉（布局镜像图片）
```

**三图（上2下1）→ 信息也是上2下1：**
```
┌───────────┬───────────┬────────┬────────┐
│  图片 A   │  图片 B   │ 信息 A │ 信息 B │
├───────────┴───────────┼────────┴────────┤
│        图片 C         │     信息 C      │
└───────────────────────┴─────────────────┘
```

**四图 2×2 网格 → 信息也是 2×2：**
```
┌───────────┬───────────┬────────┬────────┐
│  图片 A   │  图片 B   │ 信息 A │ 信息 B │
├───────────┼───────────┼────────┼────────┤
│  图片 C   │  图片 D   │ 信息 C │ 信息 D │
└───────────┴───────────┴────────┴────────┘
```

### 6.4 元数据面板内容

每张图片的信息卡片包含：

```
┌──────────────────────┐
│  文件名.png          │
├──────────────────────┤
│  尺寸    1920 × 1080 │
│  体积    2.4 MB      │
│  格式    PNG         │
│  色彩    sRGB        │
│  位深    8-bit       │
│  Alpha   有          │
├──────────────────────┤
│  ▼ EXIF 拍摄信息     │
│  相机    Canon R5    │
│  镜头    RF 85mm     │
│  光圈    f/2.8       │
│  快门    1/200s      │
│  ISO     400         │
│  时间    2024-01-15  │
├──────────────────────┤
│  ▼ 文件信息          │
│  路径    /Users/...  │
│  创建    2024-01-15  │
│  修改    2024-01-15  │
└──────────────────────┘
```

## 7. 快捷键

| 快捷键 | 功能 |
|--------|------|
| `Cmd + I` | 打开/关闭元数据面板 |
| `Cmd + L` | 切换视口锁定 |
| `Cmd + 0` | 重置为 Fit-to-View |
| `Cmd + 1` | 缩放到 100% |
| `Cmd + +/-` | 放大/缩小 |
| `Esc` | 关闭窗口 |
| `←/→` | 对比模式下切换焦点图片 |

## 8. 技术约束

1. **复用现有架构**：基于 `PanelStyle` 视觉规范，复用 `ScreenManager` 窗口定位
2. **原生实现**：使用 `ImageIO` 提取元数据，不引入第三方图片库
3. **性能要求**：冷启动到显示图片 < 500ms，元数据提取 < 50ms
4. **内存限制**：单张图片解码后不超过 100MB，超大图自动降采样

## 9. 成功指标

- 用户可在 500ms 内看到图片内容
- 并排对比时缩放/平移延迟 < 16ms (60fps)
- 元数据面板展开延迟 < 100ms

## 10. 里程碑

| 阶段 | 内容 | 预估 |
|------|------|------|
| M1 | 单图查看器 + 文件关联 | 2 天 |
| M2 | 多图并排对比 + 视口同步 | 3 天 |
| M3 | 元数据面板 | 1 天 |
| M4 | 热键流程集成 + 打磨 | 1 天 |

## 11. 开放问题

1. **默认打开方式**：是否引导用户将 Glance 设为图片默认打开方式？还是仅作为备选？
2. **最大对比数量**：支持 2 张还是 4 张？4 张会增加复杂度但更灵活
3. **视频支持**：是否复用此架构支持视频并排对比？（建议后续迭代）

---

*请审阅以上 PRD，有任何问题或调整意见请反馈。*
