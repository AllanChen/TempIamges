name: Glance
options:
  bundleIdPrefix: com.glance
  deploymentTarget:
    macOS: "12.3"
  xcodeVersion: "14.0"
  createIntermediateGroups: true

settings:
  base:
    SWIFT_VERSION: "5.0"
    MACOSX_DEPLOYMENT_TARGET: "12.3"
    CODE_SIGN_STYLE: Automatic
    ENABLE_HARDENED_RUNTIME: YES
    CODE_SIGN_IDENTITY: "-"

# Swift Package Manager 依赖配置
# 如需添加第三方库，取消注释并配置：
#
# packages:
#   # 远程依赖示例：
#   # Alamofire:
#   #   url: https://github.com/Alamofire/Alamofire
#   #   from: "5.9.0"
#   #
#   # 本地依赖示例：
#   # MyLocalPackage:
#   #   path: ../Packages/MyLocalPackage

targets:
  Glance:
    type: application
    platform: macOS
    sources:
      - Sources
    resources:
      - Resources
      - PreviewContent
    dependencies: []
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.glance.app
        INFOPLIST_FILE: Info.plist
        PRODUCT_NAME: Glance
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        LD_RUNPATH_SEARCH_PATHS:
          - "@executable_path/../Frameworks"
        SWIFT_EMIT_LOC_STRINGS: YES
        CONFIGURATION_BUILD_DIR: "$(SRCROOT)/debug/$(CONFIGURATION)"
        BUILD_ROOT: "$(SRCROOT)/debug"
    entitlements:
      path: Glance.entitlements
      properties:
        com.apple.security.get-task-allow: true